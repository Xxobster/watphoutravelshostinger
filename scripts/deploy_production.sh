#!/usr/bin/env bash
# Production deploy stub — configure Bluehost SSH details before use.
set -eu
echo "Configure DEMO_SSH_HOST and DEMO_WEB_ROOT for Bluehost staging, then use deploy_demo.py"
echo "NEVER overwrite production database after live bookings exist."
